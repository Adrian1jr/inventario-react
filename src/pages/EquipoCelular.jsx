export const EquipoCelular = () => {
  // const { data } = useFetch(
  //     "https://syntranet-last-back-end-dev-mdqm7.ondigitalocean.app/api/equipoCelular/"
  // );

  // const celulares = data ? data.results : [];

  return <></>;
};
